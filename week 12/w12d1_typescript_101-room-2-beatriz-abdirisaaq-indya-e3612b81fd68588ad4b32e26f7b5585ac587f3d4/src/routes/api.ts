import { Router } from "express";
// import userRouter from './users';

// Export the base-router
const baseRouter = Router();

// Setup routers
// Add them like the below example

// baseRouter.use('/users', userRouter);

// Export default.
export default baseRouter;
